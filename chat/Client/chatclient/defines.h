#ifndef DEFINES_H
#define DEFINES_H

enum StatusCode {NONE = -1, SUCCESS, DUPLICATE};
enum MessageType { TEXT, IMAGE, AUDIO, VIDEO };
enum MessagePurpose { INCOMING, UPCOMING, INFO };

#endif // DEFINES_H
